// Single source of truth for the product's display name. Rename here once if the
// brand ever changes again. NOTE: this is the *user-visible* name only — technical
// identifiers (the `--zm-*` design tokens, `.scale-*`/`.zm-*` CSS classes, the
// `ScaleLandingPage`/`Icon.scale`/`scaleLogo` symbols, the `z-matrix-design-system`
// package/folder) intentionally keep their existing names.
export const PRODUCT_NAME = 'Retail Expansion';
